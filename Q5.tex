\newpage
\section{Question 5}
Let $q$ be the velocity function
\[
q(z) = \frac{2i}{i - \overline{z}}.
\]
\vs \\
\begin{enumerate}[label=(\alph*)]
  \item % Part (a)
  The conjugate velocity function is
  \vs \\
  \[
  \overline{q}(z) = \overline{\frac{2i}{i - \overline{z}}} = \frac{\overline{2i}}{\overline{i\,} - z}= \frac{-2i}{-i - z} = \frac{2i}{z + i}, \quad \text{which is analytic on \ }\mathbb{C} - \{-i\}.
  \]
  \vs \\
  Hence, by HB D1 1.15 p81, $q$ is the velocity function for an ideal flow on $\mathbb{C} - \{-i\}$.
  \vvs \\
  \item % Part (b)
  A complex potential function for the flow  (i.e., a primitive of $\overline{q}$ on $\mathbb{C} - \{-i\}$) is
  \vs \\
  \[
  \Omega(z) = \int \overline{q}(z)\,dz = \int \frac{2i}{z + i}\,dz = 2i\Log (z + i).
  \]
  \vs \\
  Hence a stream function for the flow is
  \vs \\
  \begin{align*}
    \Psi(z) = \Im \Omega(z) &= \Im \Big[2i\Log(z + i)\Big] \\[4pt]
    &= \Im \Big[  2i \Big(\log \left| z + i \right| + i\Arg (z + i)\Big)\Big] \\[4pt]
    &= \Im \Big[ -2\Arg(z + i) + i \log \left| z + i\right|^2\Big] \\[4pt]
    &= \log\left| z + i\right|^2.
  \end{align*}
  \vs \\
  Now let $z = x + iy$, \ so  that \ $\left|z + i\right|^2 = \left| x + iy + i\right|^2 = x^2 + (y + 1)^2$; 

  and since on the streamlines, $\Psi(z) = k$, for $k \in \mathbb{R}$, we have
  \vs \\
  \[
  \Psi(x,y) = \log\left(x^2 + (y + 1)^2\right) = k \quad \Rightarrow \quad x^2 + (y + 1)^2 = e^k.
  \]
  \vs \\
  Finally, putting $e^k = K > 0$, we get equations for the streamlines of the flow, in $x$ and $y$:
  \vs \\
  \[
  x^2 + (y + 1)^2 = K,\quad K > 0. \quad \text{These are circles centred at the point $(0, -1)$.}
  \]
  \newpage
  \item % Part (c)
  The streamline passing through the point $1 + i$ satisfies the equation
  \vs
  \[
  \Psi(1,1) = \log\left(1^2 + (1 + 1)^2\right) = \log 5.
  \]
  So this particular streamline has equation
  \vs
  \[
  x^2 + (y + 1)^2 = 5;\quad \text{that is, a circle centred at $(0,-1)$ with radius $r = \sqrt{5}$}.
  \]
  \vs \\
  The velocity at this point is given by
  \[
  q(1 + i) = \frac{2i}{i - (1 - i)} = \frac{2i}{-1 + 2i} = \frac{2i(-1 - 2i)}{5} = \frac{4}{5} - \frac{2}{5}i.
  \]
  {
  \color{teal}
  I changed a typo in the bracket in the denominator: it said $-1-i$ rather than $1-i$
  }
  \vs \\
  Sketch of the streamline below:

  \begin{figure}[htbp]
    \centering
    % Tikz picture for Q7-ai
    \input{tikz/Q5/Q5-c}
  \end{figure}
  \vs

  \item % Part (d)
  Let $\Gamma$ be the line segment from $\alpha = 1$ to $\beta = 2 - 2i$.
  \vs \\
  By HD D1 2.2, p82, the flux is given by
  \[
  \mathcal{F}_\Gamma = \Im \Omega(\beta) - \Im \Omega(\alpha) = \Psi(\beta) - \Psi(\alpha).
  \]

  Then using $\Psi(z) = \log\left| z + i\right|^2$, from part (b), we get
  \[
  \mathcal{F}_\Gamma = \log \left| 2 - 2i + i\right|^2 - \log\left| 1 + i\right|^2 = \log( 4 + 1) - \log(1 + 1) = \log 5 - \log 2 = \log\frac{5}{2}.
  \]
\end{enumerate}
\textcolor{blue}{Do you guys agree with this answer?}
\vs \\
{
\color{teal}
Looks good to me. I had the stream function wrong from part (b), so I can't check against my answer, but checking yours, it seems correct.
}
\newpage
